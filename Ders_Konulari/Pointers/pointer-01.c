#include <stdio.h>

int main() {
    int x = 10;  // normal bir değişken

    printf("x degeri: %d\n", x);     // x'in tuttuğu değer
    printf("x adresi: %p\n", &x);    // &x -> x'in RAM'deki adresi

    return 0;
}
